// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.0
// source: group.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "molva.group.v1";

/**
 * Membership — подписанный админом состав группы; новая версия на каждое
 * изменение. Каноничная сериализация для подписи — отдельная (не protobuf).
 */
export interface Membership {
  /** 32 случайных байта */
  groupId: Uint8Array;
  version: bigint;
  name: string;
  /** Ed25519 админа (создателя) */
  adminPub: Uint8Array;
  /** NodeID участников, отсортированы байтово */
  members: Uint8Array[];
  /** Ed25519 админа над каноничной формой */
  signature: Uint8Array;
}

/**
 * SenderKeyDist — раздача своего sender key: ТЕКУЩЕЕ состояние цепочки
 * (новичок не сможет читать историю — цепочка идёт только вперёд).
 */
export interface SenderKeyDist {
  groupId: Uint8Array;
  /** растёт при rekey */
  generation: number;
  /** 32, текущий */
  chainKey: Uint8Array;
  /** номер следующего сообщения цепочки */
  n: number;
  /** Ed25519 подписи сообщений этого участника */
  signPub: Uint8Array;
}

/**
 * Welcome — приглашение новичку: членство и известные админу текущие
 * ключи участников.
 */
export interface Welcome {
  membership: Membership | undefined;
  keys: MemberKey[];
}

export interface MemberKey {
  member: Uint8Array;
  key: SenderKeyDist | undefined;
}

/** Update — новая версия членства существующим участникам. */
export interface Update {
  membership: Membership | undefined;
}

/**
 * Message — групповое сообщение (payload конверта TYPE_GROUP, открытый):
 * шифртекст под message key отправителя + его подпись.
 */
export interface Message {
  groupId: Uint8Array;
  generation: number;
  n: number;
  ciphertext: Uint8Array;
  /** Ed25519 отправителя над каноничной формой */
  signature: Uint8Array;
}

function createBaseMembership(): Membership {
  return {
    groupId: new Uint8Array(0),
    version: 0n,
    name: "",
    adminPub: new Uint8Array(0),
    members: [],
    signature: new Uint8Array(0),
  };
}

export const Membership: MessageFns<Membership> = {
  encode(message: Membership, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    if (message.version !== 0n) {
      if (BigInt.asUintN(64, message.version) !== message.version) {
        throw new globalThis.Error("value provided for field message.version of type uint64 too large");
      }
      writer.uint32(16).uint64(message.version);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.adminPub.length !== 0) {
      writer.uint32(34).bytes(message.adminPub);
    }
    for (const v of message.members) {
      writer.uint32(42).bytes(v!);
    }
    if (message.signature.length !== 0) {
      writer.uint32(50).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Membership {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMembership();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.groupId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.adminPub = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.members.push(reader.bytes());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Membership {
    return {
      groupId: isSet(object.groupId)
        ? bytesFromBase64(object.groupId)
        : isSet(object.group_id)
        ? bytesFromBase64(object.group_id)
        : new Uint8Array(0),
      version: isSet(object.version) ? BigInt(object.version) : 0n,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      adminPub: isSet(object.adminPub)
        ? bytesFromBase64(object.adminPub)
        : isSet(object.admin_pub)
        ? bytesFromBase64(object.admin_pub)
        : new Uint8Array(0),
      members: globalThis.Array.isArray(object?.members) ? object.members.map((e: any) => bytesFromBase64(e)) : [],
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: Membership): unknown {
    const obj: any = {};
    if (message.groupId.length !== 0) {
      obj.groupId = base64FromBytes(message.groupId);
    }
    if (message.version !== 0n) {
      obj.version = message.version.toString();
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.adminPub.length !== 0) {
      obj.adminPub = base64FromBytes(message.adminPub);
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => base64FromBytes(e));
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Membership>, I>>(base?: I): Membership {
    return Membership.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Membership>, I>>(object: I): Membership {
    const message = createBaseMembership();
    message.groupId = object.groupId ?? new Uint8Array(0);
    message.version = object.version ?? 0n;
    message.name = object.name ?? "";
    message.adminPub = object.adminPub ?? new Uint8Array(0);
    message.members = object.members?.map((e) => e) || [];
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSenderKeyDist(): SenderKeyDist {
  return { groupId: new Uint8Array(0), generation: 0, chainKey: new Uint8Array(0), n: 0, signPub: new Uint8Array(0) };
}

export const SenderKeyDist: MessageFns<SenderKeyDist> = {
  encode(message: SenderKeyDist, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    if (message.generation !== 0) {
      writer.uint32(16).uint32(message.generation);
    }
    if (message.chainKey.length !== 0) {
      writer.uint32(26).bytes(message.chainKey);
    }
    if (message.n !== 0) {
      writer.uint32(32).uint32(message.n);
    }
    if (message.signPub.length !== 0) {
      writer.uint32(42).bytes(message.signPub);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SenderKeyDist {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSenderKeyDist();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.groupId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.generation = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.chainKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.n = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signPub = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SenderKeyDist {
    return {
      groupId: isSet(object.groupId)
        ? bytesFromBase64(object.groupId)
        : isSet(object.group_id)
        ? bytesFromBase64(object.group_id)
        : new Uint8Array(0),
      generation: isSet(object.generation) ? globalThis.Number(object.generation) : 0,
      chainKey: isSet(object.chainKey)
        ? bytesFromBase64(object.chainKey)
        : isSet(object.chain_key)
        ? bytesFromBase64(object.chain_key)
        : new Uint8Array(0),
      n: isSet(object.n) ? globalThis.Number(object.n) : 0,
      signPub: isSet(object.signPub)
        ? bytesFromBase64(object.signPub)
        : isSet(object.sign_pub)
        ? bytesFromBase64(object.sign_pub)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SenderKeyDist): unknown {
    const obj: any = {};
    if (message.groupId.length !== 0) {
      obj.groupId = base64FromBytes(message.groupId);
    }
    if (message.generation !== 0) {
      obj.generation = Math.round(message.generation);
    }
    if (message.chainKey.length !== 0) {
      obj.chainKey = base64FromBytes(message.chainKey);
    }
    if (message.n !== 0) {
      obj.n = Math.round(message.n);
    }
    if (message.signPub.length !== 0) {
      obj.signPub = base64FromBytes(message.signPub);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SenderKeyDist>, I>>(base?: I): SenderKeyDist {
    return SenderKeyDist.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SenderKeyDist>, I>>(object: I): SenderKeyDist {
    const message = createBaseSenderKeyDist();
    message.groupId = object.groupId ?? new Uint8Array(0);
    message.generation = object.generation ?? 0;
    message.chainKey = object.chainKey ?? new Uint8Array(0);
    message.n = object.n ?? 0;
    message.signPub = object.signPub ?? new Uint8Array(0);
    return message;
  },
};

function createBaseWelcome(): Welcome {
  return { membership: undefined, keys: [] };
}

export const Welcome: MessageFns<Welcome> = {
  encode(message: Welcome, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(10).fork()).join();
    }
    for (const v of message.keys) {
      MemberKey.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Welcome {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWelcome();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.keys.push(MemberKey.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Welcome {
    return {
      membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined,
      keys: globalThis.Array.isArray(object?.keys) ? object.keys.map((e: any) => MemberKey.fromJSON(e)) : [],
    };
  },

  toJSON(message: Welcome): unknown {
    const obj: any = {};
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    if (message.keys?.length) {
      obj.keys = message.keys.map((e) => MemberKey.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Welcome>, I>>(base?: I): Welcome {
    return Welcome.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Welcome>, I>>(object: I): Welcome {
    const message = createBaseWelcome();
    message.membership = (object.membership !== undefined && object.membership !== null)
      ? Membership.fromPartial(object.membership)
      : undefined;
    message.keys = object.keys?.map((e) => MemberKey.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMemberKey(): MemberKey {
  return { member: new Uint8Array(0), key: undefined };
}

export const MemberKey: MessageFns<MemberKey> = {
  encode(message: MemberKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.member.length !== 0) {
      writer.uint32(10).bytes(message.member);
    }
    if (message.key !== undefined) {
      SenderKeyDist.encode(message.key, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MemberKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMemberKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.member = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = SenderKeyDist.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MemberKey {
    return {
      member: isSet(object.member) ? bytesFromBase64(object.member) : new Uint8Array(0),
      key: isSet(object.key) ? SenderKeyDist.fromJSON(object.key) : undefined,
    };
  },

  toJSON(message: MemberKey): unknown {
    const obj: any = {};
    if (message.member.length !== 0) {
      obj.member = base64FromBytes(message.member);
    }
    if (message.key !== undefined) {
      obj.key = SenderKeyDist.toJSON(message.key);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MemberKey>, I>>(base?: I): MemberKey {
    return MemberKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MemberKey>, I>>(object: I): MemberKey {
    const message = createBaseMemberKey();
    message.member = object.member ?? new Uint8Array(0);
    message.key = (object.key !== undefined && object.key !== null) ? SenderKeyDist.fromPartial(object.key) : undefined;
    return message;
  },
};

function createBaseUpdate(): Update {
  return { membership: undefined };
}

export const Update: MessageFns<Update> = {
  encode(message: Update, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Update {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Update {
    return { membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined };
  },

  toJSON(message: Update): unknown {
    const obj: any = {};
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Update>, I>>(base?: I): Update {
    return Update.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Update>, I>>(object: I): Update {
    const message = createBaseUpdate();
    message.membership = (object.membership !== undefined && object.membership !== null)
      ? Membership.fromPartial(object.membership)
      : undefined;
    return message;
  },
};

function createBaseMessage(): Message {
  return {
    groupId: new Uint8Array(0),
    generation: 0,
    n: 0,
    ciphertext: new Uint8Array(0),
    signature: new Uint8Array(0),
  };
}

export const Message: MessageFns<Message> = {
  encode(message: Message, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    if (message.generation !== 0) {
      writer.uint32(16).uint32(message.generation);
    }
    if (message.n !== 0) {
      writer.uint32(24).uint32(message.n);
    }
    if (message.ciphertext.length !== 0) {
      writer.uint32(34).bytes(message.ciphertext);
    }
    if (message.signature.length !== 0) {
      writer.uint32(42).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.groupId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.generation = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.n = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ciphertext = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message {
    return {
      groupId: isSet(object.groupId)
        ? bytesFromBase64(object.groupId)
        : isSet(object.group_id)
        ? bytesFromBase64(object.group_id)
        : new Uint8Array(0),
      generation: isSet(object.generation) ? globalThis.Number(object.generation) : 0,
      n: isSet(object.n) ? globalThis.Number(object.n) : 0,
      ciphertext: isSet(object.ciphertext) ? bytesFromBase64(object.ciphertext) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: Message): unknown {
    const obj: any = {};
    if (message.groupId.length !== 0) {
      obj.groupId = base64FromBytes(message.groupId);
    }
    if (message.generation !== 0) {
      obj.generation = Math.round(message.generation);
    }
    if (message.n !== 0) {
      obj.n = Math.round(message.n);
    }
    if (message.ciphertext.length !== 0) {
      obj.ciphertext = base64FromBytes(message.ciphertext);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message>, I>>(base?: I): Message {
    return Message.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message>, I>>(object: I): Message {
    const message = createBaseMessage();
    message.groupId = object.groupId ?? new Uint8Array(0);
    message.generation = object.generation ?? 0;
    message.n = object.n ?? 0;
    message.ciphertext = object.ciphertext ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
