// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.0
// source: ratchet.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "molva.ratchet.v1";

/**
 * Message — одно сообщение Double Ratchet: открытый заголовок + шифртекст.
 * Едет payload'ом конверта TYPE_CHAT.
 */
export interface Message {
  /** текущий ratchet-ключ отправителя, X25519 (32 байта) */
  dhPub: Uint8Array;
  /** длина предыдущей отправной цепочки */
  pn: number;
  /** номер сообщения в текущей цепочке */
  n: number;
  /** ChaCha20-Poly1305, ключ — одноразовый message key */
  ciphertext: Uint8Array;
}

/**
 * Init — первая половина интерактивной инициализации сессии
 * (payload конверта TYPE_SESSION_INIT).
 */
export interface Init {
  /** identity-ключ ratchet-слоя инициатора (32 байта) */
  ikPub: Uint8Array;
  /** эфемерный X25519 инициатора (32 байта) */
  ephPub: Uint8Array;
  /** 16 случайных байт, связывает init и ack */
  sessionId: Uint8Array;
}

/** InitAck — ответная половина (payload конверта TYPE_SESSION_INIT_ACK). */
export interface InitAck {
  ikPub: Uint8Array;
  ephPub: Uint8Array;
  sessionId: Uint8Array;
}

/**
 * State — персистентное состояние сессии. Содержит приватные ключи —
 * хранится только шифрованным ключом из master-seed.
 */
export interface State {
  /** root key (32) */
  rk: Uint8Array;
  /** наша текущая ratchet-пара, приватная половина (32) */
  dhsPriv: Uint8Array;
  /** их текущий ratchet-ключ (32; пусто у респондента до первого приёма) */
  dhrPub: Uint8Array;
  /** отправная цепочка (32; пусто, пока не начата) */
  cks: Uint8Array;
  /** приёмная цепочка (32; пусто, пока не начата) */
  ckr: Uint8Array;
  ns: number;
  nr: number;
  pn: number;
  /** transcript-привязка сессии (associated data) */
  ad: Uint8Array;
  skipped: SkippedKey[];
}

/** SkippedKey — отложенный ключ внеочередного сообщения. */
export interface SkippedKey {
  dhPub: Uint8Array;
  n: number;
  /** message key (32) */
  mk: Uint8Array;
}

function createBaseMessage(): Message {
  return { dhPub: new Uint8Array(0), pn: 0, n: 0, ciphertext: new Uint8Array(0) };
}

export const Message: MessageFns<Message> = {
  encode(message: Message, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dhPub.length !== 0) {
      writer.uint32(10).bytes(message.dhPub);
    }
    if (message.pn !== 0) {
      writer.uint32(16).uint32(message.pn);
    }
    if (message.n !== 0) {
      writer.uint32(24).uint32(message.n);
    }
    if (message.ciphertext.length !== 0) {
      writer.uint32(34).bytes(message.ciphertext);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dhPub = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pn = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.n = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ciphertext = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message {
    return {
      dhPub: isSet(object.dhPub)
        ? bytesFromBase64(object.dhPub)
        : isSet(object.dh_pub)
        ? bytesFromBase64(object.dh_pub)
        : new Uint8Array(0),
      pn: isSet(object.pn) ? globalThis.Number(object.pn) : 0,
      n: isSet(object.n) ? globalThis.Number(object.n) : 0,
      ciphertext: isSet(object.ciphertext) ? bytesFromBase64(object.ciphertext) : new Uint8Array(0),
    };
  },

  toJSON(message: Message): unknown {
    const obj: any = {};
    if (message.dhPub.length !== 0) {
      obj.dhPub = base64FromBytes(message.dhPub);
    }
    if (message.pn !== 0) {
      obj.pn = Math.round(message.pn);
    }
    if (message.n !== 0) {
      obj.n = Math.round(message.n);
    }
    if (message.ciphertext.length !== 0) {
      obj.ciphertext = base64FromBytes(message.ciphertext);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message>, I>>(base?: I): Message {
    return Message.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message>, I>>(object: I): Message {
    const message = createBaseMessage();
    message.dhPub = object.dhPub ?? new Uint8Array(0);
    message.pn = object.pn ?? 0;
    message.n = object.n ?? 0;
    message.ciphertext = object.ciphertext ?? new Uint8Array(0);
    return message;
  },
};

function createBaseInit(): Init {
  return { ikPub: new Uint8Array(0), ephPub: new Uint8Array(0), sessionId: new Uint8Array(0) };
}

export const Init: MessageFns<Init> = {
  encode(message: Init, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ikPub.length !== 0) {
      writer.uint32(10).bytes(message.ikPub);
    }
    if (message.ephPub.length !== 0) {
      writer.uint32(18).bytes(message.ephPub);
    }
    if (message.sessionId.length !== 0) {
      writer.uint32(26).bytes(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Init {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ikPub = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ephPub = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Init {
    return {
      ikPub: isSet(object.ikPub)
        ? bytesFromBase64(object.ikPub)
        : isSet(object.ik_pub)
        ? bytesFromBase64(object.ik_pub)
        : new Uint8Array(0),
      ephPub: isSet(object.ephPub)
        ? bytesFromBase64(object.ephPub)
        : isSet(object.eph_pub)
        ? bytesFromBase64(object.eph_pub)
        : new Uint8Array(0),
      sessionId: isSet(object.sessionId)
        ? bytesFromBase64(object.sessionId)
        : isSet(object.session_id)
        ? bytesFromBase64(object.session_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: Init): unknown {
    const obj: any = {};
    if (message.ikPub.length !== 0) {
      obj.ikPub = base64FromBytes(message.ikPub);
    }
    if (message.ephPub.length !== 0) {
      obj.ephPub = base64FromBytes(message.ephPub);
    }
    if (message.sessionId.length !== 0) {
      obj.sessionId = base64FromBytes(message.sessionId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Init>, I>>(base?: I): Init {
    return Init.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Init>, I>>(object: I): Init {
    const message = createBaseInit();
    message.ikPub = object.ikPub ?? new Uint8Array(0);
    message.ephPub = object.ephPub ?? new Uint8Array(0);
    message.sessionId = object.sessionId ?? new Uint8Array(0);
    return message;
  },
};

function createBaseInitAck(): InitAck {
  return { ikPub: new Uint8Array(0), ephPub: new Uint8Array(0), sessionId: new Uint8Array(0) };
}

export const InitAck: MessageFns<InitAck> = {
  encode(message: InitAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ikPub.length !== 0) {
      writer.uint32(10).bytes(message.ikPub);
    }
    if (message.ephPub.length !== 0) {
      writer.uint32(18).bytes(message.ephPub);
    }
    if (message.sessionId.length !== 0) {
      writer.uint32(26).bytes(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ikPub = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ephPub = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitAck {
    return {
      ikPub: isSet(object.ikPub)
        ? bytesFromBase64(object.ikPub)
        : isSet(object.ik_pub)
        ? bytesFromBase64(object.ik_pub)
        : new Uint8Array(0),
      ephPub: isSet(object.ephPub)
        ? bytesFromBase64(object.ephPub)
        : isSet(object.eph_pub)
        ? bytesFromBase64(object.eph_pub)
        : new Uint8Array(0),
      sessionId: isSet(object.sessionId)
        ? bytesFromBase64(object.sessionId)
        : isSet(object.session_id)
        ? bytesFromBase64(object.session_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: InitAck): unknown {
    const obj: any = {};
    if (message.ikPub.length !== 0) {
      obj.ikPub = base64FromBytes(message.ikPub);
    }
    if (message.ephPub.length !== 0) {
      obj.ephPub = base64FromBytes(message.ephPub);
    }
    if (message.sessionId.length !== 0) {
      obj.sessionId = base64FromBytes(message.sessionId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitAck>, I>>(base?: I): InitAck {
    return InitAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitAck>, I>>(object: I): InitAck {
    const message = createBaseInitAck();
    message.ikPub = object.ikPub ?? new Uint8Array(0);
    message.ephPub = object.ephPub ?? new Uint8Array(0);
    message.sessionId = object.sessionId ?? new Uint8Array(0);
    return message;
  },
};

function createBaseState(): State {
  return {
    rk: new Uint8Array(0),
    dhsPriv: new Uint8Array(0),
    dhrPub: new Uint8Array(0),
    cks: new Uint8Array(0),
    ckr: new Uint8Array(0),
    ns: 0,
    nr: 0,
    pn: 0,
    ad: new Uint8Array(0),
    skipped: [],
  };
}

export const State: MessageFns<State> = {
  encode(message: State, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rk.length !== 0) {
      writer.uint32(10).bytes(message.rk);
    }
    if (message.dhsPriv.length !== 0) {
      writer.uint32(18).bytes(message.dhsPriv);
    }
    if (message.dhrPub.length !== 0) {
      writer.uint32(26).bytes(message.dhrPub);
    }
    if (message.cks.length !== 0) {
      writer.uint32(34).bytes(message.cks);
    }
    if (message.ckr.length !== 0) {
      writer.uint32(42).bytes(message.ckr);
    }
    if (message.ns !== 0) {
      writer.uint32(48).uint32(message.ns);
    }
    if (message.nr !== 0) {
      writer.uint32(56).uint32(message.nr);
    }
    if (message.pn !== 0) {
      writer.uint32(64).uint32(message.pn);
    }
    if (message.ad.length !== 0) {
      writer.uint32(74).bytes(message.ad);
    }
    for (const v of message.skipped) {
      SkippedKey.encode(v!, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): State {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rk = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dhsPriv = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dhrPub = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cks = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.ckr = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.ns = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.nr = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.pn = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.ad = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.skipped.push(SkippedKey.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): State {
    return {
      rk: isSet(object.rk) ? bytesFromBase64(object.rk) : new Uint8Array(0),
      dhsPriv: isSet(object.dhsPriv)
        ? bytesFromBase64(object.dhsPriv)
        : isSet(object.dhs_priv)
        ? bytesFromBase64(object.dhs_priv)
        : new Uint8Array(0),
      dhrPub: isSet(object.dhrPub)
        ? bytesFromBase64(object.dhrPub)
        : isSet(object.dhr_pub)
        ? bytesFromBase64(object.dhr_pub)
        : new Uint8Array(0),
      cks: isSet(object.cks) ? bytesFromBase64(object.cks) : new Uint8Array(0),
      ckr: isSet(object.ckr) ? bytesFromBase64(object.ckr) : new Uint8Array(0),
      ns: isSet(object.ns) ? globalThis.Number(object.ns) : 0,
      nr: isSet(object.nr) ? globalThis.Number(object.nr) : 0,
      pn: isSet(object.pn) ? globalThis.Number(object.pn) : 0,
      ad: isSet(object.ad) ? bytesFromBase64(object.ad) : new Uint8Array(0),
      skipped: globalThis.Array.isArray(object?.skipped)
        ? object.skipped.map((e: any) => SkippedKey.fromJSON(e))
        : [],
    };
  },

  toJSON(message: State): unknown {
    const obj: any = {};
    if (message.rk.length !== 0) {
      obj.rk = base64FromBytes(message.rk);
    }
    if (message.dhsPriv.length !== 0) {
      obj.dhsPriv = base64FromBytes(message.dhsPriv);
    }
    if (message.dhrPub.length !== 0) {
      obj.dhrPub = base64FromBytes(message.dhrPub);
    }
    if (message.cks.length !== 0) {
      obj.cks = base64FromBytes(message.cks);
    }
    if (message.ckr.length !== 0) {
      obj.ckr = base64FromBytes(message.ckr);
    }
    if (message.ns !== 0) {
      obj.ns = Math.round(message.ns);
    }
    if (message.nr !== 0) {
      obj.nr = Math.round(message.nr);
    }
    if (message.pn !== 0) {
      obj.pn = Math.round(message.pn);
    }
    if (message.ad.length !== 0) {
      obj.ad = base64FromBytes(message.ad);
    }
    if (message.skipped?.length) {
      obj.skipped = message.skipped.map((e) => SkippedKey.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<State>, I>>(base?: I): State {
    return State.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<State>, I>>(object: I): State {
    const message = createBaseState();
    message.rk = object.rk ?? new Uint8Array(0);
    message.dhsPriv = object.dhsPriv ?? new Uint8Array(0);
    message.dhrPub = object.dhrPub ?? new Uint8Array(0);
    message.cks = object.cks ?? new Uint8Array(0);
    message.ckr = object.ckr ?? new Uint8Array(0);
    message.ns = object.ns ?? 0;
    message.nr = object.nr ?? 0;
    message.pn = object.pn ?? 0;
    message.ad = object.ad ?? new Uint8Array(0);
    message.skipped = object.skipped?.map((e) => SkippedKey.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSkippedKey(): SkippedKey {
  return { dhPub: new Uint8Array(0), n: 0, mk: new Uint8Array(0) };
}

export const SkippedKey: MessageFns<SkippedKey> = {
  encode(message: SkippedKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dhPub.length !== 0) {
      writer.uint32(10).bytes(message.dhPub);
    }
    if (message.n !== 0) {
      writer.uint32(16).uint32(message.n);
    }
    if (message.mk.length !== 0) {
      writer.uint32(26).bytes(message.mk);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkippedKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkippedKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dhPub = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.n = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mk = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkippedKey {
    return {
      dhPub: isSet(object.dhPub)
        ? bytesFromBase64(object.dhPub)
        : isSet(object.dh_pub)
        ? bytesFromBase64(object.dh_pub)
        : new Uint8Array(0),
      n: isSet(object.n) ? globalThis.Number(object.n) : 0,
      mk: isSet(object.mk) ? bytesFromBase64(object.mk) : new Uint8Array(0),
    };
  },

  toJSON(message: SkippedKey): unknown {
    const obj: any = {};
    if (message.dhPub.length !== 0) {
      obj.dhPub = base64FromBytes(message.dhPub);
    }
    if (message.n !== 0) {
      obj.n = Math.round(message.n);
    }
    if (message.mk.length !== 0) {
      obj.mk = base64FromBytes(message.mk);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SkippedKey>, I>>(base?: I): SkippedKey {
    return SkippedKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SkippedKey>, I>>(object: I): SkippedKey {
    const message = createBaseSkippedKey();
    message.dhPub = object.dhPub ?? new Uint8Array(0);
    message.n = object.n ?? 0;
    message.mk = object.mk ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
