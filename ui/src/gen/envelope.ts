// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.0
// source: envelope.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "molva.envelope.v1";

export enum Type {
  TYPE_UNSPECIFIED = 0,
  /** TYPE_ACK - Подтверждение приёма: payload = msg_id подтверждаемого конверта. */
  TYPE_ACK = 1,
  /** TYPE_PROBE - Presence: пустой payload, мимо outbox и истории. */
  TYPE_PROBE = 2,
  TYPE_PONG = 3,
  /** TYPE_CONTACT_REQUEST - Знакомство: payload — открытый ContactRequest/ContactAccept. */
  TYPE_CONTACT_REQUEST = 4,
  TYPE_CONTACT_ACCEPT = 5,
  /** TYPE_SESSION_INIT - Интерактивная инициализация Double Ratchet по прямому каналу. */
  TYPE_SESSION_INIT = 6,
  TYPE_SESSION_INIT_ACK = 7,
  /** TYPE_CHAT - Личное сообщение: payload — ratchet-сообщение (заголовок + ciphertext). */
  TYPE_CHAT = 8,
  /** TYPE_GROUP - Групповое сообщение: payload — sender-key ciphertext + подпись. */
  TYPE_GROUP = 9,
  /** TYPE_FILE_MANIFEST - Файловый манифест: payload — ratchet-сообщение с Manifest внутри. */
  TYPE_FILE_MANIFEST = 10,
  /**
   * TYPE_FILE_CHUNK_REQ - Оконный запрос чанков: открытый ChunkRequest, мимо outbox и дедупа
   * (pull сам ретраит — молчание лечится повтором запроса).
   */
  TYPE_FILE_CHUNK_REQ = 11,
  /** TYPE_FILE_CHUNK - Чанк файла: открытый Chunk (контент зашифрован файловым ключом). */
  TYPE_FILE_CHUNK = 12,
  /** TYPE_GROUP_WELCOME - Приглашение в группу: ratchet-сообщение с Welcome внутри. */
  TYPE_GROUP_WELCOME = 13,
  /** TYPE_GROUP_UPDATE - Новая версия членства: ratchet-сообщение с Update внутри. */
  TYPE_GROUP_UPDATE = 14,
  /** TYPE_GROUP_KEY - Раздача sender key: ratchet-сообщение с SenderKeyDist внутри. */
  TYPE_GROUP_KEY = 15,
  /** TYPE_CALL_OFFER - Сигналинг звонков: ratchet-сообщения с Offer/Answer/Hangup внутри. */
  TYPE_CALL_OFFER = 16,
  TYPE_CALL_ANSWER = 17,
  TYPE_CALL_HANGUP = 18,
  UNRECOGNIZED = -1,
}

export function typeFromJSON(object: any): Type {
  switch (object) {
    case 0:
    case "TYPE_UNSPECIFIED":
      return Type.TYPE_UNSPECIFIED;
    case 1:
    case "TYPE_ACK":
      return Type.TYPE_ACK;
    case 2:
    case "TYPE_PROBE":
      return Type.TYPE_PROBE;
    case 3:
    case "TYPE_PONG":
      return Type.TYPE_PONG;
    case 4:
    case "TYPE_CONTACT_REQUEST":
      return Type.TYPE_CONTACT_REQUEST;
    case 5:
    case "TYPE_CONTACT_ACCEPT":
      return Type.TYPE_CONTACT_ACCEPT;
    case 6:
    case "TYPE_SESSION_INIT":
      return Type.TYPE_SESSION_INIT;
    case 7:
    case "TYPE_SESSION_INIT_ACK":
      return Type.TYPE_SESSION_INIT_ACK;
    case 8:
    case "TYPE_CHAT":
      return Type.TYPE_CHAT;
    case 9:
    case "TYPE_GROUP":
      return Type.TYPE_GROUP;
    case 10:
    case "TYPE_FILE_MANIFEST":
      return Type.TYPE_FILE_MANIFEST;
    case 11:
    case "TYPE_FILE_CHUNK_REQ":
      return Type.TYPE_FILE_CHUNK_REQ;
    case 12:
    case "TYPE_FILE_CHUNK":
      return Type.TYPE_FILE_CHUNK;
    case 13:
    case "TYPE_GROUP_WELCOME":
      return Type.TYPE_GROUP_WELCOME;
    case 14:
    case "TYPE_GROUP_UPDATE":
      return Type.TYPE_GROUP_UPDATE;
    case 15:
    case "TYPE_GROUP_KEY":
      return Type.TYPE_GROUP_KEY;
    case 16:
    case "TYPE_CALL_OFFER":
      return Type.TYPE_CALL_OFFER;
    case 17:
    case "TYPE_CALL_ANSWER":
      return Type.TYPE_CALL_ANSWER;
    case 18:
    case "TYPE_CALL_HANGUP":
      return Type.TYPE_CALL_HANGUP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Type.UNRECOGNIZED;
  }
}

export function typeToJSON(object: Type): string {
  switch (object) {
    case Type.TYPE_UNSPECIFIED:
      return "TYPE_UNSPECIFIED";
    case Type.TYPE_ACK:
      return "TYPE_ACK";
    case Type.TYPE_PROBE:
      return "TYPE_PROBE";
    case Type.TYPE_PONG:
      return "TYPE_PONG";
    case Type.TYPE_CONTACT_REQUEST:
      return "TYPE_CONTACT_REQUEST";
    case Type.TYPE_CONTACT_ACCEPT:
      return "TYPE_CONTACT_ACCEPT";
    case Type.TYPE_SESSION_INIT:
      return "TYPE_SESSION_INIT";
    case Type.TYPE_SESSION_INIT_ACK:
      return "TYPE_SESSION_INIT_ACK";
    case Type.TYPE_CHAT:
      return "TYPE_CHAT";
    case Type.TYPE_GROUP:
      return "TYPE_GROUP";
    case Type.TYPE_FILE_MANIFEST:
      return "TYPE_FILE_MANIFEST";
    case Type.TYPE_FILE_CHUNK_REQ:
      return "TYPE_FILE_CHUNK_REQ";
    case Type.TYPE_FILE_CHUNK:
      return "TYPE_FILE_CHUNK";
    case Type.TYPE_GROUP_WELCOME:
      return "TYPE_GROUP_WELCOME";
    case Type.TYPE_GROUP_UPDATE:
      return "TYPE_GROUP_UPDATE";
    case Type.TYPE_GROUP_KEY:
      return "TYPE_GROUP_KEY";
    case Type.TYPE_CALL_OFFER:
      return "TYPE_CALL_OFFER";
    case Type.TYPE_CALL_ANSWER:
      return "TYPE_CALL_ANSWER";
    case Type.TYPE_CALL_HANGUP:
      return "TYPE_CALL_HANGUP";
    case Type.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Envelope — единица обмена между узлами molva поверх nodenet.
 * Уровень надёжности приложения: получатель отвечает ack'ом на каждый
 * принятый конверт, отправитель ретраит из персистентного outbox,
 * дедупликация — по (originator, msg_id).
 */
export interface Envelope {
  /** 16 случайных байт; идентификатор для ack и дедупликации. */
  msgId: Uint8Array;
  type: Type;
  /**
   * Per-sender монотонный номер сообщений истории (детект дыр).
   * Служебные конверты (ack, probe, pong) идут с нулём.
   */
  fromSeq: bigint;
  /** Лампортова метка для слияния порядка отображения в группах. */
  lamportTs: bigint;
  /**
   * Содержимое, зависящее от type: ciphertext ratchet'а/sender key,
   * либо открытый служебный payload (ack — msg_id подтверждаемого).
   */
  payload: Uint8Array;
}

function createBaseEnvelope(): Envelope {
  return { msgId: new Uint8Array(0), type: 0, fromSeq: 0n, lamportTs: 0n, payload: new Uint8Array(0) };
}

export const Envelope: MessageFns<Envelope> = {
  encode(message: Envelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.msgId.length !== 0) {
      writer.uint32(10).bytes(message.msgId);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.fromSeq !== 0n) {
      if (BigInt.asUintN(64, message.fromSeq) !== message.fromSeq) {
        throw new globalThis.Error("value provided for field message.fromSeq of type uint64 too large");
      }
      writer.uint32(24).uint64(message.fromSeq);
    }
    if (message.lamportTs !== 0n) {
      if (BigInt.asUintN(64, message.lamportTs) !== message.lamportTs) {
        throw new globalThis.Error("value provided for field message.lamportTs of type uint64 too large");
      }
      writer.uint32(32).uint64(message.lamportTs);
    }
    if (message.payload.length !== 0) {
      writer.uint32(42).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Envelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.msgId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromSeq = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lamportTs = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Envelope {
    return {
      msgId: isSet(object.msgId)
        ? bytesFromBase64(object.msgId)
        : isSet(object.msg_id)
        ? bytesFromBase64(object.msg_id)
        : new Uint8Array(0),
      type: isSet(object.type) ? typeFromJSON(object.type) : 0,
      fromSeq: isSet(object.fromSeq) ? BigInt(object.fromSeq) : isSet(object.from_seq) ? BigInt(object.from_seq) : 0n,
      lamportTs: isSet(object.lamportTs)
        ? BigInt(object.lamportTs)
        : isSet(object.lamport_ts)
        ? BigInt(object.lamport_ts)
        : 0n,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: Envelope): unknown {
    const obj: any = {};
    if (message.msgId.length !== 0) {
      obj.msgId = base64FromBytes(message.msgId);
    }
    if (message.type !== 0) {
      obj.type = typeToJSON(message.type);
    }
    if (message.fromSeq !== 0n) {
      obj.fromSeq = message.fromSeq.toString();
    }
    if (message.lamportTs !== 0n) {
      obj.lamportTs = message.lamportTs.toString();
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Envelope>, I>>(base?: I): Envelope {
    return Envelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Envelope>, I>>(object: I): Envelope {
    const message = createBaseEnvelope();
    message.msgId = object.msgId ?? new Uint8Array(0);
    message.type = object.type ?? 0;
    message.fromSeq = object.fromSeq ?? 0n;
    message.lamportTs = object.lamportTs ?? 0n;
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
