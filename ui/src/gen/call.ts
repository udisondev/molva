// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.0
// source: call.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "molva.call.v1";

export interface Offer {
  /** 16 случайных байт */
  callId: Uint8Array;
  /** предлагаемые кодеки ("opus", "vp8") */
  codecs: string[];
}

/** Ответ на оффер: принят или отвергнут. */
export interface Answer {
  callId: Uint8Array;
  accept: boolean;
}

export interface Hangup {
  callId: Uint8Array;
}

function createBaseOffer(): Offer {
  return { callId: new Uint8Array(0), codecs: [] };
}

export const Offer: MessageFns<Offer> = {
  encode(message: Offer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId.length !== 0) {
      writer.uint32(10).bytes(message.callId);
    }
    for (const v of message.codecs) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Offer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOffer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.codecs.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Offer {
    return {
      callId: isSet(object.callId)
        ? bytesFromBase64(object.callId)
        : isSet(object.call_id)
        ? bytesFromBase64(object.call_id)
        : new Uint8Array(0),
      codecs: globalThis.Array.isArray(object?.codecs) ? object.codecs.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: Offer): unknown {
    const obj: any = {};
    if (message.callId.length !== 0) {
      obj.callId = base64FromBytes(message.callId);
    }
    if (message.codecs?.length) {
      obj.codecs = message.codecs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Offer>, I>>(base?: I): Offer {
    return Offer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Offer>, I>>(object: I): Offer {
    const message = createBaseOffer();
    message.callId = object.callId ?? new Uint8Array(0);
    message.codecs = object.codecs?.map((e) => e) || [];
    return message;
  },
};

function createBaseAnswer(): Answer {
  return { callId: new Uint8Array(0), accept: false };
}

export const Answer: MessageFns<Answer> = {
  encode(message: Answer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId.length !== 0) {
      writer.uint32(10).bytes(message.callId);
    }
    if (message.accept !== false) {
      writer.uint32(16).bool(message.accept);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Answer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnswer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.accept = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Answer {
    return {
      callId: isSet(object.callId)
        ? bytesFromBase64(object.callId)
        : isSet(object.call_id)
        ? bytesFromBase64(object.call_id)
        : new Uint8Array(0),
      accept: isSet(object.accept) ? globalThis.Boolean(object.accept) : false,
    };
  },

  toJSON(message: Answer): unknown {
    const obj: any = {};
    if (message.callId.length !== 0) {
      obj.callId = base64FromBytes(message.callId);
    }
    if (message.accept !== false) {
      obj.accept = message.accept;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Answer>, I>>(base?: I): Answer {
    return Answer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Answer>, I>>(object: I): Answer {
    const message = createBaseAnswer();
    message.callId = object.callId ?? new Uint8Array(0);
    message.accept = object.accept ?? false;
    return message;
  },
};

function createBaseHangup(): Hangup {
  return { callId: new Uint8Array(0) };
}

export const Hangup: MessageFns<Hangup> = {
  encode(message: Hangup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId.length !== 0) {
      writer.uint32(10).bytes(message.callId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Hangup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHangup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Hangup {
    return {
      callId: isSet(object.callId)
        ? bytesFromBase64(object.callId)
        : isSet(object.call_id)
        ? bytesFromBase64(object.call_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: Hangup): unknown {
    const obj: any = {};
    if (message.callId.length !== 0) {
      obj.callId = base64FromBytes(message.callId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Hangup>, I>>(base?: I): Hangup {
    return Hangup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Hangup>, I>>(object: I): Hangup {
    const message = createBaseHangup();
    message.callId = object.callId ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
