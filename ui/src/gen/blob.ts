// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.0
// source: blob.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "molva.blob.v1";

/** Manifest — описание файла (внутри ratchet-шифртекста). */
export interface Manifest {
  /** 16 случайных байт */
  fileId: Uint8Array;
  /** имя файла (получатель санитизирует) */
  name: string;
  mime: string;
  /** байты */
  size: bigint;
  /** 60 КиБ в v1 */
  chunkSize: number;
  /** 32 байта, одноразовый ключ чанков */
  fileKey: Uint8Array;
  /** BLAKE2b-256 всего файла */
  wholeHash: Uint8Array;
}

/** ChunkRequest — оконный запрос недостающих чанков (получатель → отправитель). */
export interface ChunkRequest {
  fileId: Uint8Array;
  /** ≤ 64 за запрос */
  indexes: number[];
}

/**
 * Chunk — один чанк (отправитель → получатель), payload зашифрован
 * файловым ключом.
 */
export interface Chunk {
  fileId: Uint8Array;
  index: number;
  payload: Uint8Array;
}

function createBaseManifest(): Manifest {
  return {
    fileId: new Uint8Array(0),
    name: "",
    mime: "",
    size: 0n,
    chunkSize: 0,
    fileKey: new Uint8Array(0),
    wholeHash: new Uint8Array(0),
  };
}

export const Manifest: MessageFns<Manifest> = {
  encode(message: Manifest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fileId.length !== 0) {
      writer.uint32(10).bytes(message.fileId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.mime !== "") {
      writer.uint32(26).string(message.mime);
    }
    if (message.size !== 0n) {
      if (BigInt.asUintN(64, message.size) !== message.size) {
        throw new globalThis.Error("value provided for field message.size of type uint64 too large");
      }
      writer.uint32(32).uint64(message.size);
    }
    if (message.chunkSize !== 0) {
      writer.uint32(40).uint32(message.chunkSize);
    }
    if (message.fileKey.length !== 0) {
      writer.uint32(50).bytes(message.fileKey);
    }
    if (message.wholeHash.length !== 0) {
      writer.uint32(58).bytes(message.wholeHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Manifest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseManifest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fileId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mime = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.size = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.chunkSize = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.fileKey = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.wholeHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Manifest {
    return {
      fileId: isSet(object.fileId)
        ? bytesFromBase64(object.fileId)
        : isSet(object.file_id)
        ? bytesFromBase64(object.file_id)
        : new Uint8Array(0),
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      mime: isSet(object.mime) ? globalThis.String(object.mime) : "",
      size: isSet(object.size) ? BigInt(object.size) : 0n,
      chunkSize: isSet(object.chunkSize)
        ? globalThis.Number(object.chunkSize)
        : isSet(object.chunk_size)
        ? globalThis.Number(object.chunk_size)
        : 0,
      fileKey: isSet(object.fileKey)
        ? bytesFromBase64(object.fileKey)
        : isSet(object.file_key)
        ? bytesFromBase64(object.file_key)
        : new Uint8Array(0),
      wholeHash: isSet(object.wholeHash)
        ? bytesFromBase64(object.wholeHash)
        : isSet(object.whole_hash)
        ? bytesFromBase64(object.whole_hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: Manifest): unknown {
    const obj: any = {};
    if (message.fileId.length !== 0) {
      obj.fileId = base64FromBytes(message.fileId);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.mime !== "") {
      obj.mime = message.mime;
    }
    if (message.size !== 0n) {
      obj.size = message.size.toString();
    }
    if (message.chunkSize !== 0) {
      obj.chunkSize = Math.round(message.chunkSize);
    }
    if (message.fileKey.length !== 0) {
      obj.fileKey = base64FromBytes(message.fileKey);
    }
    if (message.wholeHash.length !== 0) {
      obj.wholeHash = base64FromBytes(message.wholeHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Manifest>, I>>(base?: I): Manifest {
    return Manifest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Manifest>, I>>(object: I): Manifest {
    const message = createBaseManifest();
    message.fileId = object.fileId ?? new Uint8Array(0);
    message.name = object.name ?? "";
    message.mime = object.mime ?? "";
    message.size = object.size ?? 0n;
    message.chunkSize = object.chunkSize ?? 0;
    message.fileKey = object.fileKey ?? new Uint8Array(0);
    message.wholeHash = object.wholeHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseChunkRequest(): ChunkRequest {
  return { fileId: new Uint8Array(0), indexes: [] };
}

export const ChunkRequest: MessageFns<ChunkRequest> = {
  encode(message: ChunkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fileId.length !== 0) {
      writer.uint32(10).bytes(message.fileId);
    }
    writer.uint32(18).fork();
    for (const v of message.indexes) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChunkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChunkRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fileId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.indexes.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.indexes.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChunkRequest {
    return {
      fileId: isSet(object.fileId)
        ? bytesFromBase64(object.fileId)
        : isSet(object.file_id)
        ? bytesFromBase64(object.file_id)
        : new Uint8Array(0),
      indexes: globalThis.Array.isArray(object?.indexes) ? object.indexes.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: ChunkRequest): unknown {
    const obj: any = {};
    if (message.fileId.length !== 0) {
      obj.fileId = base64FromBytes(message.fileId);
    }
    if (message.indexes?.length) {
      obj.indexes = message.indexes.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChunkRequest>, I>>(base?: I): ChunkRequest {
    return ChunkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChunkRequest>, I>>(object: I): ChunkRequest {
    const message = createBaseChunkRequest();
    message.fileId = object.fileId ?? new Uint8Array(0);
    message.indexes = object.indexes?.map((e) => e) || [];
    return message;
  },
};

function createBaseChunk(): Chunk {
  return { fileId: new Uint8Array(0), index: 0, payload: new Uint8Array(0) };
}

export const Chunk: MessageFns<Chunk> = {
  encode(message: Chunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fileId.length !== 0) {
      writer.uint32(10).bytes(message.fileId);
    }
    if (message.index !== 0) {
      writer.uint32(16).uint32(message.index);
    }
    if (message.payload.length !== 0) {
      writer.uint32(26).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Chunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fileId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Chunk {
    return {
      fileId: isSet(object.fileId)
        ? bytesFromBase64(object.fileId)
        : isSet(object.file_id)
        ? bytesFromBase64(object.file_id)
        : new Uint8Array(0),
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: Chunk): unknown {
    const obj: any = {};
    if (message.fileId.length !== 0) {
      obj.fileId = base64FromBytes(message.fileId);
    }
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Chunk>, I>>(base?: I): Chunk {
    return Chunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Chunk>, I>>(object: I): Chunk {
    const message = createBaseChunk();
    message.fileId = object.fileId ?? new Uint8Array(0);
    message.index = object.index ?? 0;
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
